#!/bin/sh

. ./deps

export GOPATH=${PWD}

for p in $packages; do

        echo "Getting $p ..."
	go get -u -d $p
        echo "Get $p"
done

for i in src/*; do
	rsync -a -z -r --exclude '.git/' --exclude '.hg/' $i ../src/
done
